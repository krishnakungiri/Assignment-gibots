<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users Report</title>
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            padding-top: 10px;
            background: whitesmoke;
        }
    </style>
</head>

<body>
    <h1 class="font-mono text-4xl text-gray-800 text-center py-10">Users Report</h1>
    <div class="container flex mx-auto">
        <div class="px-10">
            <table class="border-collapse border-2 border-gray-500">
                <thead class="border px-4 py-2">
                    <tr colspan="4" class="border-collapse border-2 border-gray-500">
                        <th colspan="4">Male</th>
                    </tr>
                    <tr>
                        <th class="border-collapse border-2 border-gray-500 w-1/3 px-4 py-2">Nationality</th>
                        <th class="border-collapse border-2 border-gray-500 w-1/3 px-4 py-2">0 - 30</th>
                        <th class="border-collapse border-2 border-gray-500 w-1/3 px-8 py-2">30 - 50</th>
                        <th class="border-collapse border-2 border-gray-500 w-1/3 px-12 py-2">50 and above</th>
                    </tr>
                </thead>
                <tbody>
                    <% male.forEach((item)=>{%>
                    <tr>
                        <td class="border-collapse border-2 border-gray-500 px-3 py-1"><%=item.nat%></td>
                        <td class="border-collapse border-2 border-gray-500 px-3 py-1"><%=item.zerothirty%></td>
                        <td class="border-collapse border-2 border-gray-500 px-3 py-1"><%=item.thirtyfifty%></td>
                        <td class="border-collapse border-2 border-gray-500 px-3 py-1"><%=item.overfifty%></td>
                    </tr>
                    <%});%>

                </tbody>
            </table>
        </div>
        <div class="px-10">
            <table class="tborder-collapse border-2 border-gray-500">
                <thead class="border-collapse border-2 border-gray-500 px-4 py-2">
                    <tr colspan="4" class="border-collapse border-2 border-gray-500 px-4 py-2">
                        <th colspan="4">Female</th>
                    </tr>
                    <tr>
                        <th class="border-collapse border-2 border-gray-500 w-1/3 px-4 py-2">Nationality</th>
                        <th class="border-collapse border-2 border-gray-500 w-1/3 px-4 py-2">0 - 30</th>
                        <th class="border-collapse border-2 border-gray-500 w-1/3 px-8 py-2">30 - 50</th>
                        <th class="border-collapse border-2 border-gray-500 w-1/3 px-12 py-2">50 and above</th>
                    </tr>
                </thead>
                <tbody>
                    <% female.forEach((item)=>{%>
                    <tr>
                        <td class="border-collapse border-2 border-gray-500 px-3 py-1"><%=item.nat%></td>
                        <td class="border-collapse border-2 border-gray-500 px-3 py-1"><%=item.zerothirty%></td>
                        <td class="border-collapse border-2 border-gray-500 px-3 py-1"><%=item.thirtyfifty%></td>
                        <td class="border-collapse border-2 border-gray-500 px-3 py-1"><%=item.overfifty%></td>
                    </tr>
                    <%});%>
                </tbody>
            </table>
        </div>
    </div>
</body>

</html>